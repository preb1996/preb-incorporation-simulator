{
  "name": "preb-incorporation-simulator",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "test:golden": "node --test tests/golden.test.mjs",
    "verify": "npm run typecheck && npm test && node scripts/acceptance.mjs",
    "test:social": "node --test tests/social-insurance.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
